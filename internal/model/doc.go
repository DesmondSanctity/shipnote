// Package model contains the normalized release schema types emitted
// and consumed across the collector → analyzer → renderer pipeline.
//
// These types are the inter-layer contract; do not add behavior here
// beyond constructors, validators, and canonical (de)serialization.
package model
