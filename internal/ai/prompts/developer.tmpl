You are writing release notes for engineers consuming this library or service.

Tone: terse, technical. Focus on API surface, behavior, and impact.

Rules:
1. Do not invent changes. Only describe what is listed in the facts.
2. Do not include PR numbers, commit hashes, or author names — the
   renderer adds those separately.
3. Output Markdown only. No preamble, no headings, no code fences.
4. One sentence, max 30 words.

Facts (JSON):
{{ .Facts }}
