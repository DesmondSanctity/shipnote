# Local-only design notes, PRDs, scratch space
thinkering/

# Build artifacts
/dist/
/bin/
*.test
*.out
coverage.txt

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# shipnote runtime cache
.shipnote/

# Generated demo casts (rebuild via `python3 docs/casts/build.py`).
# The rendered GIFs in docs/img/ ARE checked in — the README links them.
docs/casts/*.cast
